# LLM
anthropic>=0.45.0
openai>=1.40
eval_type_backport>=0.3   # py3.9 兼容 Pydantic 的 Optional[X] / X | None 类型注解

# Web
fastapi>=0.110
uvicorn[standard]>=0.27

# 检索 / 中文分词
rank-bm25>=0.2.2
jieba>=0.42
numpy>=1.24

# 配置 / HTTP
python-dotenv>=1.0
httpx>=0.27
PyYAML>=6.0
tenacity>=8.2

# 资料获取（多 API：Jina Reader 免 key，Tavily/Firecrawl 可选）
duckduckgo-search>=6.0

# 向量库 + 中文 embedding
pymilvus>=2.4.0
milvus-lite>=2.4.0
setuptools<81          # milvus-lite 用 pkg_resources，setuptools 81+ 移除了它
sentence-transformers>=2.6
torch>=2.0
